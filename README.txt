Order of operations:
1. Open "Drop statements.sql" and run to drop all previous similar named tables and sequences
2. Open "CreateTableTermProject.sql" and execute the statments to create all the tables that contain
3. Open "InsertDataTermProject.sql" to insert all the data into their respective fields and create the sequences.
4. Open the "Use Cases" folder to go retrieve each Use Case and execute them.
5. Open "Indexes.sql" to see the creation of the Indexes.